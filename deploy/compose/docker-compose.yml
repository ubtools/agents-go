# local deploy only
version: '3'
services:
  agent-eth:
    image: 'ubtr/agent-eth:${UBT_AGENTS_VERSION}'
    ports:
      - '50051:50051'
    volumes:
      - './config.yml:/config.yml'
    command: ['--config', '/config.yml', '--log', 'DEBUG', "-r"]
  am:
    image: 'ubtr/am-db:${UBT_AGENTS_VERSION}'
    ports:
      - '50052:50052'
